package myFirstProject;

import java.io.*;
import java.util.Scanner;

/*
//version:Not using transformation
public class Graphics {
	Graphics(){
	}
	public static void main(String[] args){
		String in;
		do {
		System.out.print("Calculate which graph(square/circle/triangle):");
		Scanner scanner = new Scanner(System.in);
		in = scanner.nextLine();
		if(in.equals("square")) {
			Graphics square = new Square();
			Square square1 = (Square)square;
			System.out.println("square:"+"\n"+"size="+square1.getSize(square1.input())
			+"\n"+"Circumference="+square1.getCircumference(square1.side));
		}
		if(in.equals("circle")) {
			Graphics circle = new Circle();
			Circle circle1 = (Circle)circle;
			System.out.println("circle:"+"\n"+"size="+circle1.getSize(circle1.input())
			+"\n"+"Circumference="+circle1.getCircumference(circle1.radius));
		}
		if(in.equals("triangle")) {
			Graphics triangle = new Triangle();
			Triangle triangle1 = (Triangle)triangle;
			triangle1.input();
			System.out.println("triangle:"+"\n"+"size="+triangle1.getSize(triangle1.side1, triangle1.side2, triangle1.side3)
			+"\n"+"Circumference="+triangle1.getCircumference(triangle1.side1, triangle1.side2, triangle1.side3));
		}
		}while(in.length()!=0);
		System.out.println("------The program is end------");
	}
}

class Square extends Graphics{
	double side;
	String inside;
	double size;
	double Circumference;
	public double input() {
		System.out.print("[Square]"+"input a side :");
		Scanner scanner = new Scanner(System.in);
		inside = scanner.nextLine();
		side = Double.parseDouble(inside);
		return side;
	}
	public double getSize(double side){
		size = side*side;
		return size;
	}
	public double getCircumference(double side){
		Circumference = 4*side;
		return Circumference;
	}
}

class Circle extends Graphics{
	double radius;
	String inradius;
	double size;
	double Circumference;
	public double input() {
		System.out.print("[Circle]"+"input a radius :");
		Scanner scanner = new Scanner(System.in);
		inradius = scanner.nextLine();
		radius = Double.parseDouble(inradius);
		return radius;
	}
	public double getSize(double radius){
		size = Math.PI*radius*radius;
		return size;
	}
	public double getCircumference(double radius){
		Circumference = 2*Math.PI*radius;
		return Circumference;
	}
	
}

class Triangle extends Graphics{
	double side1;
	double side2;
	double side3;
	String inside;
	double angle;
	double size;
	double Circumference;
	public void input(){
		//System.out.print("[Triangle]"+"input a side(end up with pressing ENTER) :");
		//Scanner scanner = new Scanner(System.in);
		//inside1 = scanner.nextLine();
		//side1 = Double.parseDouble(inside1);
		for(int i=1; i<4; i=i+1) {
			System.out.print("[Triangle]"+"input side"+i+"(end up with pressing ENTER) :");
			Scanner scanner = new Scanner(System.in);
			inside = scanner.nextLine();
			switch(i) {
			case 1:
				side1 = Double.parseDouble(inside);
			case 2:
				side2 = Double.parseDouble(inside);
			case 3:
				side3 = Double.parseDouble(inside);
			}
		}
	}
	public double getSize(double side1, double side2, double side3){
		angle = Math.toDegrees(Math.asin(Math.sqrt(1-Math.pow((side1*side1+side2*side2-side3*side3)/(2*side1*side2), 2))));
		size = (1.0/2.0)*side1*side2*Math.sqrt(1-Math.pow((side1*side1+side2*side2-side3*side3)/(2*side1*side2), 2));
		return size;
	}
	public double getCircumference(double side1, double side2, double side3){
		Circumference = side1+side2+side3;
		return Circumference;
	}
}
*/

//version:Using transformation

public class Graphics {
	Graphics(){
	}
	double side;
	String inside;
	double radius;
	String inradius;
	double side1;
	double side2;
	double side3;
	double angle;
	double size;
	double[] aside;
	double Circumference;
	public double input() {
		return side;
	}
	public double[] ainput() {
		return aside;
	}
	public double getSize(double side){
		return size;
	}
	public double getSize(double side1, double side2, double side3){
		return size;
	}
	public double getCircumference(double side){
		return Circumference;
	}
	public double getCircumference(double side1, double side2, double side3){
		return Circumference;
	}
	
	
	public static void main(String[] args){
		String in;
		double out;
		double[] aout;
		do {
		System.out.print("Calculate which graph(square/circle/triangle):");
		Scanner scanner = new Scanner(System.in);
		in = scanner.nextLine();
		if(in.equals("square")) {
			Graphics square = new Square();
			out = square.input();
			System.out.println("square:"+"\n"+"size="+square.getSize(out)
			+"\n"+"Circumference="+square.getCircumference(out));
		}
		if(in.equals("circle")) {
			Graphics circle = new Circle();
			out = circle.input();
			System.out.println("circle:"+"\n"+"size="+circle.getSize(out)
			+"\n"+"Circumference="+circle.getCircumference(out));
		}
		if(in.equals("triangle")) {
			Graphics triangle = new Triangle();
			aout = triangle.ainput();
			System.out.println("triangle:"+"\n"+"size="+triangle.getSize(aout[0], aout[1], aout[2])
			+"\n"+"Circumference="+triangle.getCircumference(aout[0], aout[1], aout[2]));
		}
		}while(in.length()!=0);
		System.out.println("------The program is end------");
	}
}

class Square extends Graphics{
	double side;
	String inside;
	double size;
	double Circumference;
	public double input() {
		System.out.print("[Square]"+"input a side :");
		Scanner scanner = new Scanner(System.in);
		inside = scanner.nextLine();
		side = Double.parseDouble(inside);
		return side;
	}
	public double getSize(double side){
		size = side*side;
		return size;
	}
	public double getCircumference(double side){
		Circumference = 4*side;
		return Circumference;
	}
}

class Circle extends Graphics{
	double radius;
	String inradius;
	double size;
	double Circumference;
	public double input() {
		System.out.print("[Circle]"+"input a radius :");
		Scanner scanner = new Scanner(System.in);
		inradius = scanner.nextLine();
		radius = Double.parseDouble(inradius);
		return radius;
	}
	public double getSize(double radius){
		size = Math.PI*radius*radius;
		return size;
	}
	public double getCircumference(double radius){
		Circumference = 2*Math.PI*radius;
		return Circumference;
	}
	
}

class Triangle extends Graphics{
	double side1;
	double side2;
	double side3;
	double[] side = new double[4];  //Definition should be defined completely ah
	String inside;
	double angle;
	double size;
	double Circumference;
	public double[] ainput(){
		//System.out.print("[Triangle]"+"input a side(end up with pressing ENTER) :");
		//Scanner scanner = new Scanner(System.in);
		//inside1 = scanner.nextLine();
		//side1 = Double.parseDouble(inside1);
		for(int i=1; i<4; i=i+1) {
			System.out.print("[Triangle]"+"input side"+i+"(end up with pressing ENTER) :");
			Scanner scanner = new Scanner(System.in);
			inside = scanner.nextLine();
			switch(i) {
			case 1:
				side1 = Double.parseDouble(inside);
				side[0]=side1;
			case 2:
				side2 = Double.parseDouble(inside);
				side[1]=side2;
			case 3:
				side3 = Double.parseDouble(inside);
				side[2]=side3;
			}
		}
		return side;
	}
	public double getSize(double side1, double side2, double side3){
		angle = Math.toDegrees(Math.asin(Math.sqrt(1-Math.pow((side1*side1+side2*side2-side3*side3)/(2*side1*side2), 2))));
		size = (1.0/2.0)*side1*side2*Math.sqrt(1-Math.pow((side1*side1+side2*side2-side3*side3)/(2*side1*side2), 2));
		return size;
	}
	public double getCircumference(double side1, double side2, double side3){
		Circumference = side1+side2+side3;
		return Circumference;
	}
}

